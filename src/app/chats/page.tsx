import React from 'react';
import Link from "next/link";

export default function ChatsPage() {
    return (
        <div>
            <Link href={('/')}>
                Volver
            </Link>
        </div>
    );
}